export default class Produtos{
    constructor(){
        this.elements = {};
    }
    elements(elements){
        this.elements = element
       console.log(this.elements);
    }
    elements(){
        //return this.elements;
    }
}